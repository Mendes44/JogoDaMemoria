// Este é o ponto de entrada localizado na raiz do projeto.
// `require("./src/index")` carrega e executa o arquivo que conecta as camadas.
// Com isso, o jogo pode ser iniciado diretamente com o comando `node index.js`.
require("./src/index");
